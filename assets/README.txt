Drop your photo here and name it "profile.jpg" (or update the src path
in index.html if you'd rather use a different name/format, e.g. .png).

Recommended: a square-ish or portrait photo, at least 600px on the
short side, so it stays sharp at larger screen sizes.
